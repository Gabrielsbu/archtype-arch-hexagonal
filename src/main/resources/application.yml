server:
  port: 8080

spring:
  application:
    name: loja-vendas
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/loja?createDatabaseIfNotExist=true
    username: root
    password: 1010100

